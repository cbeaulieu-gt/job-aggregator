# Preflight Smoke-Test Results

**Run date:** 2026-04-23T22:19:32Z
**Result:** all 10 sources alive — Phase B (B1-B10) cleared to proceed for every plugin.

## Classification table

| Source       | Status | HTTP | Listings returned | Notes                                                    |
| ------------ | ------ | ---- | ----------------- | -------------------------------------------------------- |
| adzuna       | green  | 200  | 1                 | Credentials valid (`ADZUNA_APP_ID`/`ADZUNA_APP_KEY`)     |
| arbeitnow    | green  | 200  | 100               | No-auth public API; smallest page returned 100 listings  |
| himalayas    | green  | 200  | 1                 | No-auth public API                                       |
| jobicy       | green  | 200  | 1                 | No-auth public API                                       |
| jooble       | green  | 200  | 30                | Credentials valid (`JOOBLE_API_KEY`); minimum page is 30 |
| jsearch      | green  | 200  | 10                | Credentials valid (`JSEARCH_API_KEY`, RapidAPI)          |
| remoteok     | green  | 200  | top-level list    | No-auth public API; returns array, not envelope          |
| remotive     | green  | 200  | 22                | No-auth public API                                       |
| the_muse     | green  | 200  | 20                | No-auth public API                                       |
| usajobs      | green  | 200  | 1                 | Credentials valid (`USAJOBS_EMAIL`/`USAJOBS_API_KEY`)    |

## What "green" means here

The Issue #4 acceptance criteria define **green** as: *HTTP 200 + ≥1 listing + `normalise()` output conforms to §9.3*.

This preflight verified the first two conditions. It did **not** run the third (`normalise()` schema conformance) because the plugins have not yet been ported to the new `JobSource` ABC — that is the work of Phase B1-B10.

So in this report:

- **green** = endpoint alive, credentials valid (where required), payload non-empty
- Full `JobRecord` schema conformance will be validated as part of each B-N migration's acceptance checklist

If a per-plugin migration in Phase B discovers a payload-shape drift that breaks normalisation, that plugin gets reclassified to **needs-fixing** at that point (and the corresponding B-N issue scope is amended).

## Outcomes for Phase B planning

- **No `broken-defer-to-v1.1` classifications** — no plugins to drop from v1.0 entry-points
- **No `needs-fixing` classifications observed at the HTTP layer** — drift, if any, will surface during normalisation work in B1-B10
- All 10 issues B1-B10 may proceed as planned

## Raw responses

Per-source raw JSON saved (gitignored) at `.tmp/preflight/<source>.json` for reference during B1-B10 normaliser implementation. Re-run `uv run python scripts/preflight_smoke.py --live` to refresh.

---

*Generated by `scripts/preflight_smoke.py` — 2026-04-23T22:19:32Z; classifications hand-validated and amended on the same date.*
